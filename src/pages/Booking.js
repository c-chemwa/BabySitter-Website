import React from 'react';
import './Booking.css'; // Add CSS styling for booking

const Booking = () => (
    <div className="booking">
        {/* Booking details and status */}
    </div>
);

export default Booking;
